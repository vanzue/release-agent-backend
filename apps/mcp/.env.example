# Required: URL of the Release Agent API server
RELEASE_AGENT_API_URL=http://localhost:3001

# Optional: Bearer token for API authentication (if the API requires it)
# RELEASE_AGENT_API_TOKEN=your-api-token

# Optional
PORT=3100
DEFAULT_REPO=microsoft/PowerToys
