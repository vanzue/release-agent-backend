{
  "name": "release-agent-backend",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "scripts"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint",
    "db:migrate": "pnpm --dir scripts db:migrate"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
